[project]
name = "github-skills-map"
version = "0.1.0"
description = "A minimal GitHub skills mapping tool."
requires-python = ">=3.8"

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-q"
testpaths = ["tests"]
